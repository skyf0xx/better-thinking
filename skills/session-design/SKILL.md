---
name: session-design
description: >
  Design a working session backwards from its intended output, choosing
  structure, participants, and format to produce that output rather than
  defaulting to a status-update meeting. Use when convening any group
  session beyond a routine sync.
type: atomic
category: collaboration
difficulty: 2
tokens: ~820
dependencies: []
related: [expectation-contracting, ideation-sprint, retrospective-facilitation]
---

# Session Design

Design a working session backwards from its intended output, choosing structure, participants, and format to produce that output rather than defaulting to a status-update meeting.

## Why

Most unproductive meetings have no designed output — they default to a generic agenda-and-discussion format regardless of purpose. Designing backward from the actual needed artifact fixes format, attendance, and time allocation together.

## Use when / Don't use when

- **Use when:** convening any group session beyond a routine sync; a recurring meeting has drifted from its original purpose.
- **Don't use when:** the need is pure information broadcast — that's an email or doc, not a session.

## Inputs → Outputs

- **Inputs:** a purpose requiring a group session.
- **Outputs:** a session design — the specific output artifact, the right participants, the format matched to that output, and a facilitation plan.

## Principles

- Define the output artifact first — a decision, a ranked list, a shared understanding, a generated option set — the format follows from this.
- Invite only who's needed for *this* output — decider, informed, and consulted are different guest lists.
- Different outputs need incompatible formats — divergent generation and convergent decision-making don't mix in one unstructured hour.
- A session with no clear output should probably be a document instead.

## Procedure

1. State the specific output artifact this session must produce.
2. Check: could this output be achieved without a synchronous session? If yes, skip the meeting.
3. Choose participants based on the output — who decides, who must be consulted, who merely needs to be informed.
4. Choose the format matched to the output type — decision needs a structured decision process; ideas need phase-gated ideation; alignment needs structured discussion with an explicit gradient check.
5. Allocate time per phase, with the output-producing phase getting the most protected time, not the leftover.
6. Close by confirming the output was actually produced, not just discussed.

## Common mistakes

- Defaulting to "let's discuss" without a target artifact.
- Inviting everyone who might be interested rather than who's needed for the output.
- Running generation and decision in the same undifferentiated block of time.

## Examples

- A design review redesigned around producing a specific decision rather than open discussion.
- A retrospective structured to output concrete action items.
- A planning meeting redesigned to actually produce a prioritized backlog.

## Related

- [[expectation-contracting]] — often the venue where this session's roles get confirmed.
- [[ideation-sprint]] — a specific session format for generative work.
- [[retrospective-facilitation]] — a specific session format for learning-focused work.
